// Description :
//    Calcul de factorielle récursive au sein d'une classe.
//
// Resultats :
//    120
//

class MathTool {
    int fact(int n) {
        if (n <= 1) {
            return 1;
        }
        return n * this.fact(n - 1);
    }
}

{
    MathTool m = new MathTool();
    println(m.fact(5));
}